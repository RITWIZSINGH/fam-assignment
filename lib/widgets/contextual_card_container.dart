import 'package:flutter/material.dart';

class ContextualCardContainer extends StatelessWidget {
  const ContextualCardContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}