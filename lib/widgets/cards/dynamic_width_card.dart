import 'package:flutter/material.dart';

class DynamicWidthCard extends StatefulWidget {
  const DynamicWidthCard({super.key});

  @override
  State<DynamicWidthCard> createState() => _DynamicWidthCardState();
}

class _DynamicWidthCardState extends State<DynamicWidthCard> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}